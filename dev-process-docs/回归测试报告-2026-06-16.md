# 回归测试报告

> **执行时间**：2026-06-16  
> **执行人**：本地 CI（pytest）  
> **分支**：`main`（模块化提交 `8b77640`..`a4a3657` 之后）  
> **环境**：Windows 10 · Python 3.12 · backend/.venv

---

## 1. 测试命令

```powershell
cd backend
$env:TMPDIR = "$PWD/.pytest_tmp"
$env:TEMP = $env:TMPDIR
$env:TMP = $env:TMPDIR
.\.venv\Scripts\python.exe -m pytest tests/ -v --basetemp=".pytest_tmp/basetemp"
```

> **说明**：Windows 下系统 Temp 目录 `pytest-of-PC` 存在权限问题，需指定项目内 `--basetemp` 方可稳定运行（20 个 setup ERROR 的根因，非业务缺陷）。

---

## 2. 汇总结果

| 指标 | 数值 |
|------|------|
| 用例总数 | **159** |
| 通过 | **159** |
| 失败 | **0** |
| 跳过 | **0** |
| 耗时 | **51.39s** |
| 警告 | 44（Pydantic/FastAPI 弃用告警，不影响功能） |

---

## 3. 模块覆盖矩阵

| 测试文件 | 覆盖 PRD 模块 | 用例数（约） | 结果 |
|----------|---------------|-------------|------|
| `test_regression_auth.py` | 注册/登录/JWT | 多 | ✅ |
| `test_regression_login_module.py` | 短信登录/RBAC/配额 | 6+ | ✅ |
| `test_regression_sessions.py` | 会话 CRUD/消息历史 | 多 | ✅ |
| `test_regression_chat.py` | SSE 流式/RAG/引用 | 多 | ✅ |
| `test_regression_knowledge.py` | 上传/删除/向量清理 | 多 | ✅ |
| `test_regression_knowledge_base.py` | 多知识库路由 | 多 | ✅ |
| `test_regression_rag_normalize.py` | 文档标准化 PDF/DOCX/MD | 20 | ✅ |
| `test_doc_asset_validator.py` | manifest 资产校验 | 4 | ✅ |
| `test_doc_normalizer_pdf.py` | PDF 解析与取消 | 2 | ✅ |
| `test_regression_intent.py` | 意图识别 | 多 | ✅ |
| `test_regression_anti_dilution.py` | 防注意力稀释 | 多 | ✅ |
| `test_regression_feedback.py` | 用户反馈 | 多 | ✅ |
| `test_feedback_analytics.py` | 反馈统计聚合 | 多 | ✅ |
| `test_regression_admin.py` | 管理后台 API | 多 | ✅ |
| `test_regression_multimodal.py` | 多模态任务 | 多 | ✅ |
| `test_regression_gateway.py` | LLM 网关/错误恢复 | 多 | ✅ |
| `test_regression_rate_limit.py` | 500 字/日配额 | 多 | ✅ |
| `test_chat_context_manager.py` | 多轮上下文截断 | 多 | ✅ |
| `test_agent_prompt_registry.py` | Agent Prompt 注册 | 多 | ✅ |

---

## 4. PRD 关键路径人工抽检建议

自动化已覆盖 API 层；以下建议演示前人工走一遍：

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 注册 → 登录 | JWT 正常，菜单按角色展示 |
| 2 | 上传 FAQ.md | 状态 processing → ready |
| 3 | 提问「退换货政策」 | SSE 逐字输出 + citations |
| 4 | 提问无关问题 | 兜底话术，无编造 |
| 5 | 连续提问 101 次 | 第 101 次 429 |
| 6 | 管理员打开反馈仪表盘 | 折线图/统计有数据 |
| 7 | 未选 KB 提问 | auto-route 命中正确库 |

结构化 QA 集：[RAG测试文档/rag_qa_testset.json](../RAG测试文档/rag_qa_testset.json)

---

## 5. 已知非阻塞项

| 项 | 级别 | 说明 |
|----|------|------|
| Pydantic `Config` class 弃用 | 低 | 框架升级项 |
| FastAPI `on_event` 弃用 | 低 | 可迁移 lifespan |
| PyPDF2 DeprecationWarning | 低 | 已引入 pypdf，兼容层告警 |
| Windows pytest Temp 权限 | 中 | 用 `--basetemp` 规避 |

---

## 6. 结论

**全量回归通过。** PRD 必做项与加分项（除终极微服务挑战）均有对应自动化用例或集成测试覆盖，可作为交付基线。
